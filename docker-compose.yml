version: '3.8'

services:
  travel-rag:
    build: .
    container_name: travel_rag_assistant
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: always
